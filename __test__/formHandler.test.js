import { handleSubmit } from '../src/client/js/formHandler'

describe('handleSubmit', () => {
  test('should be defined', () => {
    expect(handleSubmit).toBeDefined();
  });

  test('should call updateUI with correct data', async () => {
    // ...
  });
});
